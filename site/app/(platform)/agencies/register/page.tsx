import { AddPropertyForm } from "../_components/add-property-form";

/**
 * Page component that renders the AddPropertyForm.
 *
 * Renders a wrapper <div> containing the AddPropertyForm component used to register a new property.
 *
 * @returns The page's React element.
 */
export default function RegisterPropertyPage() {
  return (
    <div>
      <AddPropertyForm />
    </div>
  );
}
